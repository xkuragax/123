# 🚀 Инструкция по деплою MultiTrack Player на Timeweb Cloud

## Что тебе понадобится

1. Компьютер с Windows/Mac/Linux
2. SSH-клиент (на Windows — PuTTY или встроенный Terminal в Windows 11)
3. 30 минут времени

---

## ЧАСТЬ 1: Заказ сервера в Timeweb Cloud

### Шаг 1.1: Регистрация и вход
1. Открой https://timeweb.cloud/
2. Нажми "Войти" → "Зарегистрироваться"
3. Введи email и пароль
4. Подтверди email

### Шаг 1.2: Создание сервера
1. В панели управления нажми **"Создать сервер"**
2. Выбери локацию: **Москва**
3. Выбери тариф: **Cloud-1** (1 vCPU, 1 GB RAM, 15 GB NVMe)
4. Выбери операционную систему: **Ubuntu 22.04 LTS**
5. **Важно:** Поставь галочку **"Автоматические копии (бэкапы)"** (+20% к цене, ~58₽)
6. Нажми **"Создать сервер"**
7. Привяжи карту МИР для оплаты

### ⚠️ Шаг 1.2a: Открытие портов (ОБЯЗАТЕЛЬНО!)
По умолчанию Timeweb закрывает порты. Нужно открыть:

1. В панели Timeweb → **Облачные серверы** → твой сервер → **"Сеть"** → **"Брандмауэр"**
2. Нажми **"Добавить правило"**
3. Добавь правила:
   - **HTTP**: Направление — входящее, Протокол — TCP, Порт — 80, Источник — любой (0.0.0.0/0)
   - **HTTPS**: Направление — входящее, Протокол — TCP, Порт — 443, Источник — любой (0.0.0.0/0)
   - **SSH**: Направление — входящее, Протокол — TCP, Порт — 22, Источник — любой (0.0.0.0/0)
4. Нажми **"Применить"**

💡 **Без этого сайт не откроется в браузере!**

### Шаг 1.3: Получение доступа
После создания сервера (1-2 минуты):
1. Зайди в раздел **"Облачные серверы"**
2. Нажми на свой сервер
3. Ты увидишь:
   - **IP-адрес**: например `185.123.45.67`
   - **Логин**: `root`
   - **Пароль**: (будет показан один раз, скопируй!)

💾 **Сохрани эти данные в блокнот!**

---

## ЧАСТЬ 2: Подключение к серверу

### На Windows (через PowerShell):
1. Нажми `Win + X` → выбери **"Windows PowerShell"** или **"Терминал"**
2. Введи команду (замени IP на свой):
```bash
ssh root@185.123.45.67
```
3. Напиши `yes` и нажми Enter (первый раз)
4. Введи пароль (не отображается на экране!) и нажми Enter

### На Mac/Linux:
1. Открой Terminal
2. Введи:
```bash
ssh root@185.123.45.67
```
3. Введи пароль

✅ **Если видишь `root@server:~#` — ты внутри!**

---

## ЧАСТЬ 3: Установка всего необходимого

### Шаг 3.1: Обновление системы
Введи по очереди (копируй и вставляй):

```bash
apt update && apt upgrade -y
```

Жди завершения (может занять 2-3 минуты)

### Шаг 3.2: Установка Node.js
Введи команды по очереди:

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
```

```bash
apt install -y nodejs
```

Проверь установку:
```bash
node --version
npm --version
```

Должно показать версии (например v18.19.0 и 10.x.x)

### Шаг 3.3: Установка PM2
```bash
npm install -g pm2
```

### Шаг 3.4: Установка Git (для скачивания кода)
```bash
apt install -y git
```

---

## ЧАСТЬ 4: Загрузка и запуск проекта

### Шаг 4.1: Создание папки проекта
```bash
mkdir -p /var/www/multitrack
cd /var/www/multitrack
```

### Шаг 4.2: Загрузка файлов

**Вариант А: Через Git (если залил на GitHub)**
```bash
git clone https://github.com/ТВОЙ_АККАУНТ/multitrack-player.git .
```

**Вариант Б: Через SFTP (простой способ)**
1. Скачай программу **FileZilla** (бесплатная): https://filezilla-project.org/
2. Открой FileZilla
3. Вверху введи:
   - **Хост**: твой IP (185.123.45.67)
   - **Имя пользователя**: root
   - **Пароль**: твой пароль
   - **Порт**: 22
4. Нажми "Быстрое соединение"
5. Слева — твой компьютер, справа — сервер
6. На сервере открой папку `/var/www/multitrack`
7. Слева найди папку `timeweb-deploy` на компьютере
8. Перетащи ВСЕ файлы из папки `timeweb-deploy` вправо на сервер

### Шаг 4.3: Установка зависимостей
На сервере введи:
```bash
cd /var/www/multitrack
npm install
```

Жди (1-2 минуты). Будет много текста, это нормально.

### Шаг 4.4: Создание папок
```bash
mkdir -p data logs public/uploads
```

### Шаг 4.5: Инициализация базы данных
```bash
npm run init-db
```

Должно написать: "База данных инициализирована"

### Шаг 4.6: Запуск через PM2
```bash
pm2 start ecosystem.config.js
```

Проверь статус:
```bash
pm2 status
```

Должно быть: `online` зеленым цветом ✅

### Шаг 4.7: Настройка автозапуска
```bash
pm2 startup
```

Скопируй команду которую выведет (начинается с `sudo env PATH=...`) и вставь:

```bash
pm2 save
```

---

## ЧАСТЬ 5: Настройка Nginx (веб-сервер)

### Шаг 5.1: Установка Nginx
```bash
apt install -y nginx
```

### Шаг 5.2: Создание конфига
Введи:
```bash
nano /etc/nginx/sites-available/multitrack
```

Откроется редактор. Вставь этот текст (замени `185.123.45.67` на свой IP):

```nginx
server {
    listen 80;
    server_name 185.123.45.67;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /uploads/ {
        alias /var/www/multitrack/public/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    client_max_body_size 500M;
}
```

**Сохранение:**
1. Нажми `Ctrl + O` (буква О)
2. Нажми `Enter`
3. Нажми `Ctrl + X` для выхода

### Шаг 5.3: Активация сайта
```bash
ln -s /etc/nginx/sites-available/multitrack /etc/nginx/sites-enabled/
rm /etc/nginx/sites-enabled/default
nginx -t
systemctl restart nginx
```

---

## ЧАСТЬ 6: Настройка SSL (HTTPS)

### Шаг 6.1: Установка Certbot
```bash
apt install -y certbot python3-certbot-nginx
```

### Шаг 6.2: Получение сертификата
```bash
certbot --nginx -d 185.123.45.67
```

Если спросит email — введи свой.

Если спросит про редирект — выбери вариант 2 (Redirect).

✅ Теперь сайт работает на HTTPS!

---

## ЧАСТЬ 7: Проверка работы

1. Открой браузер
2. Введи: `https://185.123.45.67` (замени на свой IP)
3. Должна открыться страница с надписью "Каталог альбомов"

🎉 **Поздравляю! Всё работает!**

---

## ЧАСТЬ 8: Подключение своего домена (опционально)

Если купил домен (например `mymusic.ru`):

1. В панели управления доменом добавь A-запись:
   - Имя: `@`
   - Значение: твой IP (185.123.45.67)

2. На сервере:
```bash
certbot --nginx -d mymusic.ru -d www.mymusic.ru
```

3. Готово! Сайт доступен по домену.

---

## ЧАСТЬ 9: Как обновлять код

Если внёс изменения в код:

1. Загрузи новые файлы через FileZilla
2. На сервере выполни:
```bash
cd /var/www/multitrack
pm2 restart multitrack-player
```

---

## Полезные команды (сохрани себе)

```bash
# Посмотреть статус
pm2 status

# Перезапустить
pm2 restart multitrack-player

# Остановить
pm2 stop multitrack-player

# Логи
pm2 logs

# Логи в реальном времени
pm2 logs --follow

# Перезапуск Nginx
systemctl restart nginx

# Проверка ошибок Nginx
nginx -t
```

---

## Что делать если что-то не работает

### Сайт не открывается
1. Проверь статус: `pm2 status`
2. Посмотри логи приложения: `pm2 logs multitrack-player`
3. Проверь Nginx: `systemctl status nginx`

💡 **Команда `pm2 logs multitrack-player` покажет ошибки в реальном времени — используй её, чтобы понять в чём проблема!**

### Ошибка 502 Bad Gateway
```bash
pm2 restart multitrack-player
systemctl restart nginx
```

### Не загружаются файлы
```bash
chmod 755 /var/www/multitrack/public/uploads
```

### Забыл пароль от сервера
В панели Timeweb: Облачные серверы → твой сервер → "Сбросить пароль"

---

## 💰 Итоговая стоимость

- Сервер Cloud-1: **290₽/мес**
- Автобэкапы (+20%): **~58₽/мес**
- **Итого: ~348₽/мес** (~$3.5)

---

**Готов начать? Начинай с Части 1! Удачи! 🚀**
