# ✅ Чеклист перед загрузкой на сервер

## 📦 Что нужно сделать на сервере после установки Ubuntu

### 1. Установить Node.js 18+
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs
```

### 2. Установить PM2
```bash
npm install -g pm2
```

### 3. Загрузить файлы проекта
Через FileZilla или SCP загрузи ВСЕ файлы из папки `timeweb-deploy` в `/var/www/multitrack`

### 4. Запустить установку
```bash
cd /var/www/multitrack
bash setup.sh
```

Или вручную:
```bash
cd /var/www/multitrack
mkdir -p data logs public/uploads
npm install
npm run init-db
pm2 start ecosystem.config.js
pm2 save
```

### 5. Проверить что работает
```bash
curl http://localhost:3000
pm2 status
```

---

## 🔧 Исправленные проблемы

| Проблема | Исправление |
|----------|-------------|
| `?.` синтаксис (Node 18 не поддерживает) | Заменён на `&&` в `albums.js:37` и `tracks.js:24` |
| Лишний `storage.js` с AWS | Удалён (не использовался) |
| Отсутствие папок `data/`, `logs/` | Создаются автоматически через `setup.sh` или `npm run setup` |

---

## 📁 Структура файлов для загрузки

```
/var/www/multitrack/
├── server/
│   ├── app.js           ✅ Проверен
│   ├── database.js      ✅ Проверен
│   └── routes/
│       ├── albums.js    ✅ Исправлен (?. → &&)
│       ├── songs.js     ✅ Проверен
│       ├── tracks.js    ✅ Исправлен (?. → &&)
│       └── upload.js    ✅ Проверен
├── public/
│   ├── index.html       ✅ Проверен
│   ├── css/
│   │   └── styles.css   ✅ Проверен
│   ├── js/
│   │   ├── app.js       ✅ Проверен
│   │   └── MultitrackPlayer.js  ✅ Проверен
│   └── uploads/         📁 Создаётся автоматически
├── scripts/
│   └── init-db.js       ✅ Проверен
├── package.json         ✅ Обновлён (убран aws-sdk)
├── ecosystem.config.js  ✅ Проверен
├── setup.sh             ✅ Новый скрипт установки
├── .gitignore           ✅ Новый
├── README.md            ✅ Документация
├── DEPLOY-GUIDE.md      ✅ Подробная инструкция
└── COMMANDS-CHEATSHEET.md  ✅ Шпаргалка
```

---

## ⚠️ Важно!

1. **Не используй старую папку** `/var/www/multitrack` на сервере — удали её и создай заново:
   ```bash
   rm -rf /var/www/multitrack
   mkdir -p /var/www/multitrack
   ```

2. **Права доступа**: Убедись что папка `public/uploads` доступна для записи:
   ```bash
   chmod 755 /var/www/multitrack/public/uploads
   ```

3. **Проверка перед запуском**:
   ```bash
   node -e "console.log('Node OK:', process.version)"
   ```

---

## 🚀 Быстрый старт (после переустановки Ubuntu)

```bash
# 1. Обнови систему
apt update && apt upgrade -y

# 2. Установи Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 3. Установи PM2
npm install -g pm2

# 4. Загрузи файлы (через FileZilla в /var/www/multitrack)

# 5. Запусти установку
cd /var/www/multitrack
bash setup.sh

# 6. Проверь
pm2 status
curl http://localhost:3000
```

---

## 🆘 Если не работает

1. **Ошибка "Cannot find module"**:
   ```bash
   cd /var/www/multitrack
   rm -rf node_modules
   npm install
   ```

2. **Ошибка прав доступа**:
   ```bash
   chmod -R 755 /var/www/multitrack
   ```

3. **Порт 3000 занят**:
   ```bash
   pm2 kill
   pm2 start ecosystem.config.js
   ```

4. **Смотри логи**:
   ```bash
   pm2 logs
   ```

---

**Готово к деплою! 🎉**