# 🎵 Multitrack Player - Cloudflare Deployment Guide

## 📋 Что тебе понадобится

1. **Аккаунт Cloudflare** (бесплатный) - https://dash.cloudflare.com/sign-up
2. **Node.js** версии 18 или выше - https://nodejs.org (скачай LTS версию)
3. **Твои аудио файлы** (767 МБ как ты сжал)
4. **15 минут времени** и внимательность

---

## 🚀 ШАГ 0: Установи Node.js (если ещё не установлен)

### Windows:
1. Иди на https://nodejs.org
2. Жми большую зелёную кнопку "LTS" (слева)
3. Скачивается установщик
4. Запускай, жми "Next" пока не установится
5. Перезагрузи компьютер

### Mac:
1. Иди на https://nodejs.org
2. Жми "LTS" для macOS
3. Устанавливай как обычную программу

### Проверь что работает:
Открой терминал (Windows: CMD или PowerShell, Mac: Terminal) и напиши:
```bash
node --version
```
Должно показать версию, типа `v18.17.0`

---

## 🔐 ШАГ 1: Создай аккаунт Cloudflare

1. Иди на https://dash.cloudflare.com/sign-up
2. Введи свою почту и придумай пароль
3. Подтверди почту (зайди в почту, нажми ссылку)
4. **Важно:** Cloudflare НЕ требует карту для бесплатного тарифа!

---

## 📦 ШАГ 2: Подготовка проекта

### 1. Открой терминал
**Windows:** Нажми `Win+R`, напиши `cmd`, нажми Enter
**Mac:** Открой Spotlight (`Cmd+Space`), напиши `Terminal`

### 2. Перейди в папку проекта
```bash
cd путь/до/папки/cloudflare-project
```
Например:
```bash
cd C:\Users\ТвоеИмя\Desktop\cloudflare-project
```
или на Mac:
```bash
cd ~/Desktop/cloudflare-project
```

### 3. Установи зависимости
```bash
npm install
```
Будет много текста, подожди пока закончится.

---

## 🔧 ШАГ 3: Логин в Cloudflare

### 1. Залогинься через терминал:
```bash
npx wrangler login
```

### 2. Откроется браузер
- Выбери свой аккаунт Cloudflare
- Нажми "Authorize"
- Закрой вкладку

### 3. В терминале должно написать "Successfully logged in"

---

## 🗄️ ШАГ 4: Создай базу данных (D1)

Это бесплатная SQLite база данных в облаке.

```bash
npx wrangler d1 create multitrack-db
```

В консоли появится что-то типа:
```
✅ Successfully created DB 'multitrack-db'

[[d1_databases]]
binding = "DB"
database_name = "multitrack-db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

**Важно:** Скопируй `database_id` (длинная строка с дефисами) в блокнот!

---

## 📝 ШАГ 5: Обнови конфигурацию

### 1. Открой файл `wrangler.toml` в папке проекта
Можно через Блокнот (Windows) или TextEdit (Mac)

### 2. Найди эту строку:
```toml
database_id = "ЗАМЕНИТЬ_НА_ID_ИЗ_КОНСОЛИ"
```

### 3. Замени на твой ID:
```toml
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```
(вставь тот что скопировал в шаге 4)

### 4. Сохрани файл (`Ctrl+S` или `Cmd+S`)

---

## 🪣 ШАГ 6: Создай хранилище файлов (R2)

```bash
npx wrangler r2 bucket create multitrack-files
```

Если напишет "Bucket already exists" - это нормально, значит уже создано.

---

## 🔑 ШАГ 7: Установи пароль админа

### Вариант 1 - Простой (через конфиг):
Открой `wrangler.toml` и найди:
```toml
ADMIN_PASSWORD = "admin123"
```
Замени на свой пароль:
```toml
ADMIN_PASSWORD = "ТВОЙ_ПАРОЛЬ"
```

### Вариант 2 - Безопасный (через секреты):
```bash
npx wrangler secret put ADMIN_TOKEN
```
Тебя спросят пароль - введи и нажми Enter.

---

## 📊 ШАГ 8: Примени миграции базы данных

Это создаст таблицы в базе данных:

```bash
npx wrangler d1 migrations apply multitrack-db
```

Должно написать "Migrations applied successfully!"

---

## 🚀 ШАГ 9: Деплой! (Загрузка на сервер)

### 1. Запусти деплой:

**Windows:**
```bash
scripts\deploy.bat
```

**Mac/Linux:**
```bash
bash scripts/deploy.sh
```

Или вручную:
```bash
# Сначала API (Worker)
npx wrangler deploy

# Затем фронтенд (Pages)
npx wrangler pages deploy frontend --project-name=multitrack-player
```

### 2. Жди...
Будет много текста, процесс занимает 1-2 минуты.

### 3. Готово!
В конце будет ссылка типа:
- **API:** `https://multitrack-api.xxx.workers.dev`
- **Сайт:** `https://multitrack-player.pages.dev`

**Сохрани эти ссылки!**

---

## 📤 ШАГ 10: Загрузи свои файлы

### Способ 1: Через админку (рекомендую)
1. Открой сайт по ссылке из шага 9
2. Перейди на `/login`
3. Введи пароль админа
4. Создай альбомы, песни, загружай стемы через браузер

### Способ 2: Если есть JSON с данными
Если у тебя есть файл с данными (например, из старой базы):

```bash
node scripts/import-data.js путь/к/данным.json
```

Формат JSON:
```json
{
  "albums": [...],
  "songs": [...],
  "tracks": [...]
}
```

---

## 🔄 Как обновлять (после изменений)

Если ты что-то поменял в коде:

```bash
# Быстрое обновление Worker (API)
npx wrangler deploy

# Быстрое обновление Pages (фронтенд)
npx wrangler pages deploy frontend --project-name=multitrack-player

# Всё вместе
npm run deploy:full
```

---

## 🆘 Частые проблемы

### "database_id is invalid"
- Проверь что скопировал правильный ID в wrangler.toml
- ID должен быть в кавычках

### "Authentication error"
- Залогинься заново: `npx wrangler login`

### "Bucket already exists"
- Это не ошибка, просто продолжай

### Файлы не загружаются
- Проверь что R2 bucket создан
- Проверь права доступа в консоли Cloudflare

---

## 📱 Доступ к сайту

После деплоя твой сайт доступен по адресу:
```
https://multitrack-player.pages.dev
```

Админка:
```
https://multitrack-player.pages.dev/login
```

---

## 💰 Сколько это стоит?

**Бесплатный тариф Cloudflare включает:**
- ✅ Workers: 100,000 запросов/день
- ✅ Pages: Неограниченный трафик
- ✅ D1: 5 GB хранения (базы данных)
- ✅ R2: 10 GB хранения (файлы)

Твои 767 МБ влезают полностью бесплатно!

---

## 🎯 Структура проекта

```
cloudflare-project/
├── worker/           # Backend API (Cloudflare Worker)
│   ├── index.js     # Основной код API
│   └── router.js    # Роутер запросов
├── frontend/        # Сайт (Cloudflare Pages)
│   ├── index.html   # Главная страница
│   ├── css/         # Стили
│   └── js/          # JavaScript код
├── migrations/      # SQL миграции базы данных
├── scripts/         # Скрипты для деплоя
├── wrangler.toml    # Конфигурация
└── package.json     # Зависимости
```

---

## 📞 Нужна помощь?

1. Проверь что все шаги выполнены по порядку
2. Перечитай ошибку в терминале - там обычно всё написано
3. Убедись что не пропустил кавычки или запятые в конфигах

---

**Удачи! 🎉**
